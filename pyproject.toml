[project]
name = "astro"
version = "0.1.0"
description = "Real-sky astrology birth chart CLI — Python port of Nuastro."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "skyfield>=1.54",
    "typer>=0.26.7",
]

[project.scripts]
astro = "nuastro.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true

[tool.hatch.build.targets.wheel]
packages = ["nuastro"]
